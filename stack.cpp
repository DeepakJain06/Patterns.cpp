#include <bits/stdc++.h>
using namespace std;
int main(){
    stack<int> st;
    st.push(455);
    st.push(34);
    st.pop();
    cout << st.size() << endl;
    st.push(2);
    cout << st.size() << endl;
    st.pop();
    cout << st.size() << endl;
    st.push(3);
    cout << st.size() << endl;
    
// printing the stack

    stack<int> st1, st2;
    st1.swap(st2);
    for (int val : )
        cout << val << " ";

        return 0;
}